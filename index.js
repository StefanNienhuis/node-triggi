var request = require('request');

class Triggi {

  constructor(connectURL) {
    console.error('Due to the renaming of Triggi, i also renamed my package. This package will no longer work. Install the new package by using the command: npm install olisto --save')
  }
}

module.exports = Triggi;
